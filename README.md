# netwatch
Network diagnostics and Wi-Fi management CLI for Linux, written in Go. Uses native networking APIs, ICMP, DNS, TCP, Netlink and D-Bus/NetworkManager instead of wrapping existing CLI tools.
